# Third-party notices for @aloneio/runmesh-protocol

This npm package does not bundle third-party source code. At version 0.1.0 it
declares the following external runtime dependency:

- [`zod` 4.4.3](https://www.npmjs.com/package/zod/v/4.4.3), MIT.

npm installs dependencies separately. Its upstream license file and notices
must be retained when it is redistributed. This package's PolyForm
Noncommercial License 1.0.0 does not relicense `zod` or any other dependency.
For source-tree scope and the lockfile inventory basis, see the repository-level
`THIRD_PARTY_NOTICES.md` in the source repository.
